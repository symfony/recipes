monolog:
    handlers:
        # Enable if you need logs during (automated) tests
        #main:
        #    type: stream
        #    path: "%kernel.logs_dir%/%kernel.environment%.log"
        #    level: debug
        #    channels: ["!event"]
