# Enable stateless CSRF protection for forms, logins/logouts and user switching
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            - logout
            - switch_user
