name: Q&A

on:
    pull_request_target:

defaults:
    run:
        shell: bash

jobs:
    call-qa:
        uses: symfony/recipes/.github/workflows/callable-qa.yml@5b2dcec
        secrets:
            token: ${{ secrets.BOT_TOKEN }}
